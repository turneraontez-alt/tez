"""Win-probability model: :mod:`features` (H2H / form / common-opponent),
:mod:`win_prob` (hand-set logistic blend), :mod:`calibration` (versioned
Platt transform, identity until enough graded matches exist)."""
